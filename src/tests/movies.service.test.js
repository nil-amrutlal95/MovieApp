const MovieService = require('../services/movies.service');

const movieService = new MovieService();

describe('MovieService: Search by keyword', () => {
    test
})


describe('MovieService: Search by ID', () => {
    test
})