const MovieController = require('../controllers/movies.controller');

describe('Movie Controller : search by ID', () => {
    test('Controller Validators', async () => {
        const mockRequest = {};
        mockRequest.params.query = {
            
        };
    })

})